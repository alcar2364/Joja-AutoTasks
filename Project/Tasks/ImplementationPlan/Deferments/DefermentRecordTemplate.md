## Deprecated

This legacy template is retained temporarily as a compatibility reference.

Use the unified Implementation Issues record template instead:

- `Project/Tasks/ImplementationPlan/ImplementationIssues/IssueRecordTemplate.md`

Legacy deferments are now represented as `type: Deferment` inside the `Implementation Issues` system.
