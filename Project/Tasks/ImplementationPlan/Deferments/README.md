## Deprecated

The legacy `Deferments` folder is kept temporarily as a compatibility reference.

The canonical issue-tracking system is now:

- `Project/Tasks/ImplementationPlan/ImplementationIssues/README.md`
- `Project/Tasks/ImplementationPlan/ImplementationIssues/ImplementationIssuesIndex.md`
- `Project/Tasks/ImplementationPlan/ImplementationIssues/ImplementationIssuesArchive.md`

Existing `DEF-###` records are migrated into the new `Implementation Issues` system as `type: Deferment` records.

Do not add new active tracking records under `Deferments/`.
